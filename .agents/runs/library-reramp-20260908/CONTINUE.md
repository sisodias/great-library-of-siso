# Library reramp — cold continuation

Purpose: make the Library a useful human/agent front door to independently owned
research, capabilities and projects. Registry identity, immutable evidence and
private source boundaries remain authoritative. This checkpoint is a review
branch, not a deployed whole-project completion.

Branch: `library/reramp-20260908`, repository `sisodias/great-library-of-siso`.
Read this file, `artifacts/checkpoint.json`, `docs/library-ui-integration.md`, then
the existing task, without replaying the original recovery:

```sh
node bin/gls task TASK-0007
node bin/gls tasks
```

Accepted: shared frame for all generated readers, 20 preserved authored documents,
truthful selection/owner-entry projections, read-only discovery/task CLI, useful
Runtime/Action Model/Harness/Shell entries and a private SISO Source stub. Tasks
0008/0009 hold accepted CLI and cold-Luna contribution receipts. Historical tasks
0001/0002/0006 were reconciled against already-merged main commits; do not repeat.

Next justified node is **finish TASK-0007 visual acceptance**, not a new redesign.
From this exact branch, use Node20 and the existing commands:

```sh
npm ci
npm run verify
node scripts/audit-ui-integration.mjs --strict
python3 -m http.server 8974 --bind 127.0.0.1 --directory site
```

Open `/use/`, then root, Work, question, industry/value, promotion and document
readers. Check 2000×1250, 390px mobile, 200% text, keyboard, no JS, denied storage
and reduced motion. Six representative routes and core focus/collapse checks
already have evidence; repeat only for changed bytes or uncovered behavior.
The initial root mobile overflow was fixed; the earlier failing capture remains
as history. `artifacts/checkpoint.json` names the final fresh-read result.

One-shot Luna packet: read TASK-0007 and this checkpoint; own only one demonstrated
reader defect and its narrow test/style fix; preserve all peer edits; do not
change registry identity or immutable records. Return reproduction, exact paths,
check/exit and before/after proof. The orchestrator independently verifies the
result before task completion. A native goal may track that bounded unit; no new
runner, task store, recursive fanout or invented slash command is required.

Stop the affected unit if source is missing, ownership conflicts, tests fail
without a understood cause, or authority is absent. Do not repeat an unchanged
failed attempt. Preserve the evidence and report the remaining gate. No automatic
research, skill installation, private data ingestion or provider/runtime changes.

No active helper or gls claim is retained at checkpoint. Preview is stopped; start
the command above when needed. Public main/merge/deploy is forbidden without new
authority. Branch pushes do not trigger this repository's main-only Pages job.
PR #21/#23/#24 and shell #1 remain separately reviewable; this branch carries
reviewed relevant work but does not merge those PRs publicly. Production missing
routes currently fall back to the homepage; the new 404 artifact needs real
production readback after authorized publication.
