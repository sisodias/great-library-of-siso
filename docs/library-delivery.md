# Library delivery — 8 September 2026

## Where it belongs

The canonical repository is `sisodias/great-library-of-siso`, branch `main`.
The normal reading surface is https://great-library-of-siso.pages.dev/ and the
human/agent operating guide is https://great-library-of-siso.pages.dev/use/.
The temporary review worktree was implementation isolation, not a separate
product. Historical run receipts remain under `.agents/runs/`.

Published source: `430ace3c499c90b74dc6c2dac4e29c744972819e`.
Cloudflare deployment and stable alias passed exact readback; see
[evidence](evidence/library-release-20260908.json).

## Delivered in this release

The earlier Library owner built the root/Foundry pilot, industry readers and
guarded registration tooling. Fable and Opus then built the separate shared
shell, 24-family template bank and improved parts. Today's work integrated that
existing source into the Library and repaired its reading/operating entry; it
did not invent or rebuild the whole template bank.

Existing template references, recovered and inspected during this run:

- Gallery: https://siso-shell.pages.dev/
- Project example: https://siso-shell.pages.dev/t/U4/example/
- Work example: https://siso-shell.pages.dev/t/U3/example/
- Question example: https://siso-shell.pages.dev/t/U5/example/
- Parts fixture: https://siso-shell.pages.dev/t/U13/example/

These are template demonstrations with dated inputs, not current project or
owner observations. The Library consumes exact source revision
`10895acfbd79bfa21698c2ba4cd160e2410ac141`; a newer gallery deployment would not
silently upgrade that pin.

- Shared CRM-derived navigation across 98 HTML readers, including 20 authored
  documents with original text, links and anchors preserved.
- `/use/`, `llms.txt`, current-task reading and bounded `gls search`, `inspect`,
  `tasks` and `task` commands using existing registry/task sources.
- Explicit selected-versus-registered state, source visibility and owning entry
  instructions in the catalogue and dossiers. Stale projections fail closed.
- Reviewed Runtime, Action Model, Harness Lab, Shell and private SISO Source
  entry pages. Private corpus and runtime source stay with their owners.
- One independently reviewed cold-Luna Runtime contribution and three historical
  completed tasks reconciled against main, preventing repeated completed work.
- V41 selects the exact reviewed shell revision while preserving 31 other Release
  selections and the Assembly. Vendor assets and attribution remain intact.

## Verification and honest limits

The full Node20 Library gate passed at checkpoint; source validation, all-route
frame checks, document parity, link checks and publication scanning passed on
final checkpoint bytes. Release checks add desktop/mobile coverage across page
families, the Promotion overflow correction, and an operating-page 200% zoom
check. Existing no-JS, denied-storage, collapse persistence and focus checks pass.
These checks are not a claim of exhaustive visual approval of every page or
browser. Historical failures and exact checks remain in the run artifacts.

The optional GitHub Pages mirror is **not deployed**. Its original root-only
Foundry navigation assertion was fixed for the repository URL prefix. Run
`34183140834` then passed the build/test stages but failed `configure-pages` with
404 because Pages was not enabled/configured for Actions. No account setting
was changed. The primary Cloudflare deployment above is independently verified.

No automatic skill training, fleet efficiency multiplier, live agent dashboard,
or general-purpose autonomous project completion is claimed. Action Model and
Harness project status is attributed to their owners. Private Work metadata does
not admit or disclose their payloads.

## How the next agent starts

```sh
node bin/gls tasks
node bin/gls search "agent runtime"
node bin/gls inspect siso-agent-base
```

Inspect the exact open task returned by `tasks`. A Luna can take one bounded
source-reading or implementation task, with scope, acceptance and stop conditions;
the owner independently reviews its result. Reuse each project's existing
execution machinery. Do not create a new task registry or repeat unchanged failures.

Remaining meaningful outcomes are deeper source-backed core-project entries,
accepted cold-agent use of those methods, and a coordinated shared-component
upgrade exercised by a second consumer. Each requires its own source and owner
acceptance; the UI alone does not establish them.

## Learning and higher-intelligence pickup

**Critical undelivered work:** many Works still have only source locators rather
than reviewed usage instructions. Shared navigation does not solve missing
substance. Cross-consumer component upgrades have not been demonstrated end to
end. Activity remains dated registry/task data, not verified live agent presence.
The demonstrated cold-Luna contribution is one bounded case, not proof that a
lower-capability orchestrator can operate the whole ecosystem unattended.

The important invalidated assumptions were that a registered Release means a
consumer uses it; that HTTP 200 means the requested Work exists; that a green
build establishes visual acceptance; and that copied CLI help is executable
instruction. Evidence separated all four: the old shell stayed pinned, missing
routes returned homepage HTML, browser checks caught layout failures, and owner
review rejected shell-redirection placeholders in a proposed Runtime entry.
These were corrected locally and verified within the scopes described above.

A highest-capability or GitHub/Web UI reviewer should ask: **Does a reader now
have enough truthful context to choose and use the right capability, or have we
only made incomplete information easier to navigate? Which missing source-backed
entry or shared-component contract would unlock the next accepted real outcome?**
Judge this against the original human purpose, not the number of pages/templates
or the size of the test suite. Another gallery or new task store is not the
default answer. Broader framework/skills claims must come from the owning project.

GitHub-visible pickup: this report, `CURRENT_STATE.md`,
`docs/library-ui-integration.md`, `docs/gls.md`, the existing `.agents/tasks/`
records, and `.agents/runs/library-reramp-20260908/artifacts/checkpoint.json`.
Use `docs/evidence/library-release-20260908.json` for public release truth.
The original recovery and signoff reports under that run directory are historical
context; their review-only/publication-hold passages are explicitly superseded.

Private source conversations, detailed owner evidence and local screenshots are
not available merely because a reviewer can read GitHub. The public records
preserve their relevant boundaries without copying those payloads. Request an
authorized source when it is essential, and keep missing evidence explicit.
Lunas can inspect one exact public source, draft one grounded entry or fix one
reproduced defect; the owner resolves semantics and independently accepts it.
No new implementation, skill promotion, experiment, release or direction follows
from this learning checkpoint. Wait for Shaan to choose the next active lane.

Publication receipts are recorded in CURRENT_STATE.md after exact deployed
readback. GitHub main pushes also trigger the configured GitHub Pages mirror.
